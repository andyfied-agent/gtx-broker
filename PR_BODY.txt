All fixes applied. 26 tests pass.

## Bug Fixes

### Worker Status Persistence
- **Fixed**: `get_all_workers()` now properly maps SQLite `availability` column to `status` key for `WorkerProfile.from_dict()`
- **Fixed**: `select_worker()` now correctly filters out BUSY, UNAVAILABLE, and MAINTENANCE workers
- All workers now return their persisted status, not default AVAILABLE

### State Machine Enforcement
- **Fixed**: `retry_task()` now validates that only `running` or `awaiting_review` states can transition to `retry_wait`
- **Fixed**: `cancel_task()` now validates that only `queued`, `claimed`, `retry_wait`, or `awaiting_review` states can be cancelled
- States like `running`, `succeeded`, `failed_terminal` are properly rejected

### Event Logging
- **Fixed**: `claim_task()` now emits `task_claimed` event with proper `from_state` and `to_state`
- All state transitions now properly logged to `task_events` table

### Atomic Storage
- **Fixed**: `stage_input()` now assembles complete task directory under `tmp/` first, then atomically renames to `incoming/`
- Prevents partial task exposure before staging is complete
- **Fixed**: All `datetime.utcnow()` calls replaced with `datetime.now(timezone.utc)` (deprecation fix)

### CI/Testing
- **Fixed**: Test fixtures now use `Path(__file__).resolve().parents[1]` for repository root (not hardcoded paths)
- Works on GitHub runners and any environment

## New State Transitions Implemented
- `requeue_retry_wait()`: `retry_wait` → `queued`
- `requeue_awaiting_review()`: `awaiting_review` → `queued`
- `transition_running_to_awaiting_review()`: `running` → `awaiting_review`
- `transition_awaiting_review_to_failed()`: `awaiting_review` → `failed_terminal`

## Tests Added
- 26 comprehensive tests (up from 17)
- New tests for:
  - Worker status persistence and selection
  - State machine enforcement for retry/cancel
  - Event logging for claim_task, retry_task, cancel_task
  - Atomic directory creation in staging
  - All new state transitions

All tests pass. Ready for review.
